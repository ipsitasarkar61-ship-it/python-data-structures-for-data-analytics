---

## `split_equal_sum.py`

```python
def can_split_equal_sum(arr):
    """
    Check if the array can be split into two contiguous parts
    having equal sum.
    """

    total_sum = sum(arr)

    # If total sum is odd, equal split is impossible
    if total_sum % 2 != 0:
        return False

    prefix_sum = 0

    for i in range(len(arr)):
        prefix_sum += arr[i]

        # Check if remaining sum equals prefix sum
        if prefix_sum == total_sum - prefix_sum:
            return True

    return False


# Example usage
if __name__ == "__main__":
    arr = [1, 2, 3, 3]
    result = can_split_equal_sum(arr)
    print("Can split into equal sum parts:", result)